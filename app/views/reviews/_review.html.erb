<% play.reviews.each do |review| %>
  <p><%= review.rating %></p>
  <p><%= review.comment %></p>
  <%= link_to 'Edit', edit_play_review_path(play, review) %>
                                     <!-- (paly, review) play 從哪來的？  -->
  <%= link_to 'Trash It', play_review_path(play, review), method: :delete, data: { confirm: '確定刪除評論嗎，如果確定請按，確認' } %>
  <hr>
<% end %>